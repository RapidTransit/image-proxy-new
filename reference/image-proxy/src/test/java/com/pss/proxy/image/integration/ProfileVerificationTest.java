package com.pss.proxy.image.integration;


import io.vertx.core.Vertx;
import io.vertx.junit5.VertxExtension;
import io.vertx.junit5.VertxTestContext;
import org.junit.jupiter.api.extension.ExtendWith;

@ExtendWith(VertxExtension.class)
public class ProfileVerificationTest {

    public void testProfiles(Vertx vertx, VertxTestContext context){
//        ImageProxyVerticle imageProxyVerticle = new ImageProxyVerticle();
//        vertx.deployVerticle(, context.succeeding(h-> {
//            Context ctx = vertx.getOrCreateContext();
//        });
    }
}
